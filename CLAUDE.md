# CLAUDE.md

このリポジトリで作業するエージェント向けの運用ルール。

## リリース（タグ付与を忘れない）

Releases のスキル zip はタグ push でしか更新されない。**PRのマージだけでは配布物は古いままになる。**

- `.claude-plugin/plugin.json` の `version` を上げる変更が main にマージされたら、**必ずそのコミットにタグ `v<version>` を付与して push する**（例: version 2.5.0 → `git tag v2.5.0 <mainのHEAD> && git push origin v2.5.0`）。バージョンを上げた本人（エージェント）がマージ確認後に実施する。
- タグ push で `.github/workflows/release-skill-zips.yml` が走り、リリースに全スキルの zip と `kuroco-skills-all.zip` が添付される。README のダウンロードリンクは `releases/latest` を指す。
- version 据え置きでスキルの内容だけ直した場合は、`gh workflow run release-skill-zips.yml -f tag=v<現行version> --ref main` で既存タグのアセットを作り直す。
- スキルを変更するPRでは `.claude-plugin/plugin.json` の `version` と対象スキルの `metadata.version` を上げる（機能追加は minor、修正は patch）。

## SKILL.md の制約

- frontmatter の `description` に山かっこ（`<...>`）を含めない。claude.ai のアップロード時に XML タグと判定され拒否される（本文は対象外）。

## SKILL.md の書き方

### 賞味期限のあるものを恒久ルールと混ぜない

Kuroco 本体やツール側の不具合・制約に対する**回避策**は、`## 暫定回避策` として独立した節にまとめる。
手順やアンチパターンの表に紛れ込ませない。

- 回避策には**解消条件**を書く。書かないと、直ったあとも誰も消せない
- 解消条件は**外から観測できる挙動**で書く（「このツールが〇〇を返すようになったら不要」）。**Kuroco 本体の Issue / PR 番号は書かない**——このリポジトリは公開で、読み手は参照できない
- 恒久ルール（設計判断・書き方の規範）は本文に置く。両者が同じ表に並ぶと、読み手はどちらが今も有効か判断できない
- 上流修正が入ったら節ごと削除する

### 判断は SKILL.md、リファレンスは references/

SKILL.md は「何をどう判断するか」を持つ層。**APIレスポンスのキー名・フィールド対応表・設定値の一覧は
`references/` に置き、SKILL.md からは1行で参照する。**

- キー名の類は Kuroco 側の変更で古くなる。本文に埋めると、修正のたびに判断のロジックまで読み直すことになる
- 1つの表セルに複数の関心事（入口の判断・レスポンスの構造・サイズの注意・保存方針）を詰めない。判断が埋もれる
- 目安: `security-audit`（SKILL 198行 / references 312行）・`api-performance-review`（275 / 777）の比率

### エラーには回避策ではなく手順の修正で応える

遭遇したエラーの「回避方法」をトラブルシューティングに追記するPRは、そのままでは受け付けない。
エラーは症状であって、直すべきは症状に至らせた手順のほう。

- エラーから得た知見は、該当する手順の節に**事前の正しい手順**として書く。正しい手順が書かれていれば、エラー項目そのものが不要になる（例:「OAuth 認証は実TTYとブラウザが必要なので、ユーザー自身のローカル端末で実行する」は接続手順に書く。`stdin isn't a terminal` の行を表に足すのではなく）
- トラブルシューティング表に追記してよいのは「**症状 → 仕様上の原因 → 原因を取り除く対処**」の3点が揃うものだけ。「別の環境で実行する」「ユーザーに手動でやってもらう」「やり直す・再起動する」は原因を残した迂回であり、表の行にしない
- エラーに遭遇したら、まずスキル本文の手順がそのエラーへ誘導していないかを疑う。誘導しているなら手順側を直す
- 迂回しか対処が存在しない場合、それは上流（Kuroco 本体・ツール側）の不具合・制約なので、「賞味期限のあるものを恒久ルールと混ぜない」に従い `## 暫定回避策` に解消条件つきで置く
- インシデントごとに回避行を足すと、表が事故ログ化して仕様に根ざした行が埋もれる。表は仕様の索引であって、遭遇記録ではない
